# PARALLAX Screenshot Plan

Target set: 4–5 screenshots
Source: production v2, Sites version 7
Do not include private UI, account information, local paths, unrelated extension chrome, or stale production versions without verification.

## 1. BROKEN X-Ray

State:

- Subly — BROKEN.
- v2 · production.
- Exact goal visible.
- Audit completed.

Crop:

- Header and goal card.
- Semantic X-Ray from Human Intent through Semantic Outcome.
- Technical/semantic result contrast.
- First issue row if legible.

Caption/use:

> Technical PASS / HTTP 200. Semantic FAIL / Intent violated.

Purpose: the primary thesis image.

## 2. FIXED X-Ray

State:

- Same exact goal.
- Subly — FIXED.
- v2 · production.
- Audit completed.

Crop:

- Path showing recommend_plan.
- Recommendation/no-mutation result.
- Intent PASS, Parity PASS, Agency WARN.
- Semantic qualifier and evidence summary.

Caption/use:

> Same goal, safer semantic path; the honest Agency WARN remains visible.

Purpose: prove before/after without changing the user goal.

## 3. Tagboard policy/effect result

State:

- External validation selector on Tagboard rejected.
- Authority and evidence labels visible.

Crop:

- POLICY REJECT.
- EFFECT PREVENTED.
- Technical PASS.
- No issue count that implies a technical error.

Caption/use:

> Policy REJECT is not technical failure; the domain effect was prevented.

Purpose: prove v2 separates policy outcome from execution status.

## 4. The Archive complementary result

State:

- The Archive external validation record.
- Authority and evidence labels visible.

Crop:

- Human/Agent surface relation showing COMPLEMENTARY.
- Relevant X-Ray and matrix row.
- No misleading presentation of complementarity as a defect.

Caption/use:

> Semantic parity is not surface equality.

Purpose: prove intentional Human/Agent asymmetry is representable.

## 5. Native WebMCP invocation

State:

- Production v2.
- Chrome 151 native WebMCP environment.
- Fresh native run with structured result.

Crop:

- Native tool discovery or tool list showing the five PARALLAX meta-tools.
- Structured run_parity_audit result.
- Visible Agent Execution Log entry: WebMCP invocation · structured result returned.

Caption/use:

> Native WebMCP invocation returned structured audit data and updated the visible log.

Purpose: prove the demo uses real WebMCP, not only internal simulation.

## Capture rules

- Verify Sites version 7 before capture.
- Keep the reviewed source SHA in the capture log, not over the UI.
- Use 16:9 and readable text.
- Do not show a real account, private repository, SIWC token, or unrelated browser warning.
- Do not use a previous capture as version-7 evidence without provenance.
- Save the raw capture name, timestamp, URL, selected context, model version, and whether the view is live or captured.
